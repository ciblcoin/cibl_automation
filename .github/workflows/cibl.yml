name: 🚀 CIBL Automation

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

permissions:
  contents: write

jobs:
  main:
    runs-on: ubuntu-latest
    
    steps:
    # ۱. Checkout با PAT
    - name: 📥 Checkout
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.PAT_TOKEN }}
    
    # ۲. Setup
    - name: ⚙️ Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    
    # ۳. Create files
    - name: 🚀 Create files
      run: |
        mkdir -p public
        
        # پست نمونه
        echo "🚀 CIBL Token Test" > public/post.txt
        echo "💰 Price: \$0.00012345" >> public/post.txt
        echo "📈 24h Change: +5.25%" >> public/post.txt
        echo "#CIBL #Solana" >> public/post.txt
        
        # داده‌ها
        echo '{"price": 0.00012345, "status": "test"}' > public/data.json
        
        echo "✅ Files created"
    
    # ۴. Test PAT
    - name: 🔍 Test PAT
      run: |
        if [ -z "${{ secrets.PAT_TOKEN }}" ]; then
          echo "❌ PAT_TOKEN not set in Secrets"
          exit 1
        else
          echo "✅ PAT_TOKEN is configured"
          echo "Token exists: Yes"
        fi
    
    # ۵. Commit
    - name: 💾 Commit
      run: |
        git config user.email "cibl-auto@users.noreply.github.com"
        git config user.name "CIBL Automation"
        git add public/
        git commit -m "🤖 Test commit" || echo "No changes"
    
    # ۶. Push با PAT
    - name: 📤 Push
      run: git push
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
    
    # ۷. Success
    - name: ✅ Success
      run: |
        echo "🎉 TEST SUCCESSFUL!"
        echo "PAT_TOKEN is working correctly"
        echo "Next: Run the real automation"
